specVersion: "v1beta"
serviceId: "code"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "code-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
